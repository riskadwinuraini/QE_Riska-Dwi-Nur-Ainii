cara membuat branch yaitu menggunakan 
- git checkout -b (nama branch), atau
- git branch (nama branch)

cara push pake branch baru yaitu
- git add .
- git commit -am "kepsyen"
- git push origin (nama branch baru)